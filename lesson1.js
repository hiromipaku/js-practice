// 配列を作る
let fruits = ['apple', 'banana', 'orange'];

// pushで追加
fruits.push('grape');

// popで最後の要素削除
fruits.pop();

// shiftで先頭削除
fruits.shift();

// unshiftで先頭に追加
fruits.unshift('melon');

console.log(fruits); // 出力: ['melon', 'banana', 'orange']