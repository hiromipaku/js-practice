// 配列を作る
let animals = ['dog', 'cat', 'bird', 'lion'];

// pushで追加
animals.push('panda');

// popで最後の要素削除
animals.pop();

// shiftで先頭削除
animals.shift();
console.log(animals);

// unshiftで先頭に追加
animals.unshift('bear');

// 出力
console.log(animals);
